pre-commit:
  parallel: true
  commands:
    typecheck:
      run: npm run typecheck
    lint:
      glob: "*.{js,jsx,ts,tsx,json,jsonc}"
      run: npm run lint:staged
    format:
      glob: "*.{js,jsx,ts,tsx,json,jsonc}"
      run: npm run fmt:staged

commit-msg:
  commands:
    lint-commit-msg:
      run: npx --no-install commitlint --edit "$1"

pre-push:
  parallel: true
  commands:
    typecheck:
      run: npm run typecheck
    lint:
      glob: "*.{js,jsx,ts,tsx,json,jsonc}"
      run: npm run lint
    format:
      glob: "*.{js,jsx,ts,tsx,json,jsonc}"
      run: npm run fmt
    tests:
      run: npm run test:cov
    are-the-types-wrong:
      run: npm run attw
